package Filemanager::Config;

our $config = {
  base_path => '/home/USER/SITE/Filemanager/', # I don't even think this is used... 
  uploads_directory => "<!-- tmpl_var uploads_directory -->", #Absolute path to where the root of your files are
  url_path => "<!-- tmpl_var url_path -->" #The root that the user thinks the files are at
}
